package be.ugent.vopro1.adapter.action.executor.validation;

import be.ugent.vopro1.adapter.action.Action;

/**
 * The representation of a validation
 */
public class ValidationAction extends Action {
    /* Currently, validation requires no extra parameters, but it might require some
     * later on
     */
}
